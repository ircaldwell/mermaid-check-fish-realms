# Check fish observations against known ranges (MERMAID and GBIF)
Code example to check whether fish species are outside known ranges, based on a dataset with species names and locations (latitude and longitude), compared against realms in the MERMAID fish attributes database and GBIF. 
